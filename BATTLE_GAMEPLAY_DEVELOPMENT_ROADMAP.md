# 局内玩法下一阶段交接路线图

> 更新于 2026-08-24

本阶段只推进两个内容：奖励房和 Boss 房。实现顺序固定为先完成奖励房闭环，再实现 Boss 战，最后进行整局流程验收。

## 一、奖励房

### 1. 明确房间流程

1. 战斗房清空后只记录玩家尚未使用的奖励选择点数，并允许玩家进入奖励房。
2. 奖励房不能再以“没有怪物的 Fighting 房间”表示。为奖励房增加明确的运行阶段，推荐新增 `Rewarding`（或统一重命名为 `RewardSelection`）并同步 C++、protobuf 和客户端状态映射。
3. 进入奖励房后生成本轮候选；拥有点数的在线玩家可以连续完成自己的选择，点数为零后清空当前候选。
4. 奖励阶段的推进条件只依赖仍在线且存活、且本轮确实有待选择点数的玩家。掉线玩家不阻塞房间，也不被代选；其点数和未完成状态保留到后续奖励选择阶段。
5. 所有符合条件的在线玩家完成选择后进入出口选择，再按房间图进入下一个房间。

### 2. 奖励数据与效果

1. 建立独立的奖励定义表：稳定 ID、奖励类别、数值、等级成长、可重复选择规则和展示信息。候选生成使用房间随机种子，保证重连后候选不漂移。
2. 首批奖励至少包含回血、增加伤害和增加防御；防御奖励必须接入统一伤害结算路径，明确减伤顺序、上限和叠加规则。
3. 选择接口需要校验房间阶段、玩家身份、候选 ID、剩余点数和重复提交；重复请求不能重复扣除点数或重复施加效果。
4. 奖励结果写入玩家运行时状态和快照，至少同步当前点数、当前候选、已获得奖励及等级，保证断线重连后可以继续选择。

### 3. 房间运行时改造

1. 在 `RoomRuntime` 和 `BattleInstance` 中按房间类型分派进入、tick、清理和完成逻辑：奖励房只创建布局和交互物，不创建怪物、不产生击杀事件。
2. 将“战斗结束触发祝福选择”的逻辑改为“进入奖励房触发奖励阶段”，避免战斗房和奖励房共享一套隐含状态。
3. 奖励房切换时清理上一房间实体和过期候选，但不能清理玩家的永久奖励、等级和待选择点数。
4. 出口选择沿用房间图的合法出口校验；奖励房完成后应能稳定进入 Boss 房，且重复选择、过期选择不会重复切房。

### 4. 协议与客户端交互

1. 快照补齐当前房间类型/布局、奖励阶段剩余时间、每个玩家的候选和完成状态；不要让客户端通过“实体数量为零”推断奖励房。
2. 保留现有选择请求的兼容入口；若奖励类别不再局限于祝福，新增通用奖励选择消息时必须同时更新 `proto/`、服务端映射和客户端解析，再运行协议生成脚本。
3. 客户端至少实现奖励候选展示、逐点选择、已获得效果展示、等待出口和重连恢复四种状态，并对非法或过期选择给出明确错误。

### 5. 奖励房验收条件

- 进入奖励房不会生成怪物，玩家不会因空房间被误判为战斗胜利。
- 多个玩家拥有不同点数时可以独立完成选择；一个玩家掉线不会阻塞其他在线玩家进入出口选择。
- 奖励效果在后续战斗中生效，快照和重连后的候选、点数、已选结果一致。
- 重复请求、非法候选、超时和跨房间请求均不会重复发奖或破坏房间状态。

## 二、Boss 房

### 1. Boss 数据与实体模型

1. 在 `MonsterKind`/配置层增加稳定的 Boss 标识和独立配置，不再用普通近战怪物代替 Boss。
2. 配置 Boss 的生命、碰撞体、移动、攻击伤害、攻击范围、冷却、硬直、阶段阈值和掉落/结算标记；数值集中放在玩法配置，不散落在系统代码中。
3. 为 Boss 增加专用组件或行为状态，至少能表达当前阶段、技能冷却、施法目标、攻击序列和受击免疫窗口。

### 2. Boss 战斗逻辑

1. 复用现有伤害、死亡、事件和实体快照链路；Boss 只在 AI 决策、技能编排和阶段切换处引入特殊逻辑。
2. 首个 Boss 先实现一个可验证的核心循环：预警、锁定目标、多方向投射物/范围攻击、结算伤害、进入冷却。所有攻击必须使用 tick 驱动，不能依赖客户端时间。
3. 明确投射物生命周期、重复命中保护、离开房间边界后的清理、Boss 死亡时的技能取消和场上实体回收规则。
4. 按生命阈值切换至少一个阶段，阶段切换要产生事件并同步到快照，避免客户端只看到数值突变。

### 3. Boss 房流程与结算

1. Boss 房进入时创建 Boss 和布局实体，状态进入 Fighting；不能进入普通奖励选择流程。
2. Boss 死亡是 Boss 房唯一的胜利触发条件；胜利后生成最终结算并结束房间。所有玩家死亡仍然判定失败。
3. Boss 房没有后继出口时，必须保证“Boss 死亡事件、房间清空、胜利结算、`FinishMatch`”只执行一次且顺序稳定。
4. 结算统计明确 Boss 击杀归属、玩家击杀统计、胜负原因和奖励发放，避免 Boss 事件被普通怪物统计逻辑吞掉。

### 4. 协议与表现

1. 快照中的 Boss 实体需要稳定的 `monster_kind`/场景对象标识、当前生命、阶段和必要的技能状态。
2. 为预警、技能释放、阶段切换和 Boss 死亡增加可回放的战斗事件；客户端断线重连后以快照为准，不依赖错过的瞬时特效事件。
3. 客户端完成 Boss 血条、阶段表现、投射物/范围预警、死亡和结算界面；表现层不能承担命中或伤害判定。

### 5. Boss 房验收条件

- Boss 能稳定生成、移动、攻击、受伤和死亡，普通怪物规则仍不受影响。
- 多方向攻击在不同 tick 率和重连后不会重复命中或永久残留。
- 阶段切换、Boss 死亡和最终结算各只发生一次；胜利/失败结果与 rcenter 结算一致。
- 从奖励房进入 Boss 房、完成 Boss 战并结束整局的完整链路可重复运行。

## 三、推荐实施顺序

1. 先完成奖励房状态机、奖励数据模型和快照/选择接口。
2. 接入回血、伤害、防御三类奖励，并完成奖励房到 Boss 房的切房链路。
3. 再实现 Boss 实体、AI、投射物技能和阶段事件。
4. 最后进行完整流程验收：战斗房 -> 奖励房 -> Boss 房 -> 胜利结算，并在内容稳定后再安排性能优化。
